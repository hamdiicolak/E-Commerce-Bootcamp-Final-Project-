@model ProductViewModel

@{
    ViewData["Title"] = "UserProductDetail";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<head>
	<link rel="stylesheet" href="mystyle.css">
</head>

<body>

	<div class="height d-flex justify-content-center align-items-center">

		<div class="card p-5">

			<div class="d-flex justify-content-between align-items-center ">
				<div class="mt-2">
					<h4 class="text-uppercase">@Model.Name</h4>
					<div class="mt-5">
						<h5 class="text-uppercase mb-0">@Model.Brand</h5>
						<h6 class="main-heading mt-0">@Model.UserName</h6>
						<h6 class="main-heading mt-0">@Model.Price</h6>
					</div>
				</div>
				<div class="image">
					<img src="@Url.Content("/uploadedFiles/Images/" + @Model.ImageUrl)" height="200" />
				</div>
			</div>

			<div class="d-flex justify-content-between align-items-center mt-2 mb-2">
				<span>Renk : @Model.Color</span>
			</div>


			<p>@Model.UsingStatus</p>
			<p>@Model.Description</p>

			@*<div class="form-group">

				<a class="btn btn-danger" asp-controller="Products" asp-action="BuyProduct" asp-route-id="@Model.Id" role="button">
					Satın Al
				</a>

				<a class="btn btn-danger" asp-controller="Products" asp-action="BuyProduct" asp-route-id="@Model.Id" role="button">
					Satın Al
				</a>
			</div>*@
		</div>

	</div>


</body>
